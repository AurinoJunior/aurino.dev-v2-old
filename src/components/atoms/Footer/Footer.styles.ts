import styled from "styled-components";
import { gray_800, secondary } from "../../../styles/Colors";

export const Paragraph = styled.p`
  text-align: center;
  font-weight: 600;
  font-size: 1rem;

  padding: 1rem 0;
  margin-top: 3rem;

  border-top: solid 1px ${gray_800};
`;
